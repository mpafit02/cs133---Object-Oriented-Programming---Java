package homework3;

/**
 * The class Fish creates objects to feed the civilians
 * 
 * Constructors: Fish(int health) // calls the super constructor with the
 * parameter health to initialize the value
 * 
 * @author Marios
 *
 */
public class Fish extends Resources {
	// Constructors
	/**
	 * Creates a Fish with the parameter of health
	 * @param health;
	 */
	public Fish(int health) {
		super(health);
	}
}