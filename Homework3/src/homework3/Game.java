package homework3;

/**
 * The class Game creates objects to feed the civilians
 * 
 * Constructors: Game(int health) // calls the super constructor with the
 * parameter health to initialize the value
 * 
 * @author Marios
 *
 */
public class Game extends Resources {
	// Constructors
	/**
	 * Creates a Game with the parameter of health
	 * @param health;
	 */
	public Game(int health) {
		super(health);
	}
}
